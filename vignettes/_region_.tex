\message{ !name(Speed.Rnw.tex)}
\message{ !name(Speed.Rnw) !offset(77) }
library(pROC)
library(ROCR)
with(sets$test, {
  guess <- predict(fit, features)
  microbenchmark(WeightedROC={
    WeightedROC(guess, label)
  }, ROCR={
    pred <- prediction(guess, label)
    performance(pred, "tpr", "fpr")
  }, pROC={
    roc(guess, label, algorithm=2)
  })
})

\message{ !name(Speed.Rnw.tex) !offset(-17) }
